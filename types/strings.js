"use strict";
(() => {
    var _a;
    const batman = "Batman";
    const greenLantern = "Green Lantern";
    const superman = `Superman`;
    console.log(`I'am, ${greenLantern}`);
    console.log(batman.toLocaleUpperCase());
    console.log(((_a = batman[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) || "Dont exist");
})();
